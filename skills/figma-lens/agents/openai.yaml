interface:
  display_name: "Figma Lens"
  short_description: "Headless Figma design inspection over CLI or MCP"
  default_prompt: "Use $figma-lens to inspect the supplied Figma URL, open every relied-on screenshot through actual vision input, catalog boards before focusing representative states, resolve exact typography, inspect important children at source size, reuse exact assets, and verify visible evidence."
